"use strict";

/** Creates a user. */
function makeUser(name, email, age, isMale, ethnicity, address) {
    return {
        'name':name,
        'email':email,
        'age':age,
        'isMale':isMale,
        'ethnicity':ethnicity,
        'address':address
    };
}